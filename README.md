# 骗子酒馆

2–4 人浏览器联机小游戏，提供骰子与卡牌两种模式。当前版本包含摘/飞、真假豹子、轮盘后全员重摇、开局规则面板、全窗口摸鱼遮罩和游戏结算排行榜。

## 启动

```bash
npm install
npm start
```

浏览器打开 `http://localhost:3000`。可通过环境变量 `PORT` 和 `ROOM_CODE` 覆盖默认端口与四位房间号。

## 测试

```bash
npm test
```

核心骰子规则放在 `game-logic.js`，由服务端统一校验；玩家的原始骰子和手牌不会通过公共房间状态广播给其他客户端。
